apiVersion: v1
kind: Namespace
metadata:
  name: cmi-naap

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: incident-correlation-config
  namespace: cmi-naap
data:
  LOG_LEVEL: "INFO"
  SERVICE_PORT: "8005"
  ENVIRONMENT: "staging"
  MODEL_PATH: "/models"
  ELASTICSEARCH_HOST: "elasticsearch:9200"
  PROMETHEUS_ENABLED: "true"
  METRICS_PORT: "9090"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-correlation-rca
  namespace: cmi-naap
  labels:
    app: incident-correlation-rca
    component: ai-ml
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: incident-correlation-rca
  template:
    metadata:
      labels:
        app: incident-correlation-rca
    spec:
      containers:
      - name: incident-correlation-rca
        image: ghcr.io/cmi/incident-correlation-rca:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8005
          protocol: TCP
        - name: metrics
          containerPort: 9090
          protocol: TCP
        envFrom:
        - configMapRef:
            name: incident-correlation-config
        resources:
          requests:
            cpu: 2000m
            memory: 2Gi
          limits:
            cpu: 4000m
            memory: 4Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2

---
apiVersion: v1
kind: Service
metadata:
  name: incident-correlation-rca
  namespace: cmi-naap
  labels:
    app: incident-correlation-rca
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8005
    targetPort: 8005
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: incident-correlation-rca

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: incident-correlation-rca-hpa
  namespace: cmi-naap
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: incident-correlation-rca
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: incident-correlation-rca-network-policy
  namespace: cmi-naap
spec:
  podSelector:
    matchLabels:
      app: incident-correlation-rca
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: cmi-naap
    ports:
    - protocol: TCP
      port: 8005
    - protocol: TCP
      port: 9090
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: cmi-naap
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
