apiVersion: v1
kind: Namespace
metadata:
  name: cmi-naap

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: autonomous-ops-config
  namespace: cmi-naap
data:
  LOG_LEVEL: "INFO"
  SERVICE_PORT: "8002"
  ENVIRONMENT: "staging"
  KAFKA_BROKERS: "kafka-cluster:9092"
  MODEL_PATH: "/models"
  PROMETHEUS_ENABLED: "true"
  METRICS_PORT: "9090"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: autonomous-operations-agent
  namespace: cmi-naap
  labels:
    app: autonomous-operations-agent
    component: ai-ml
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: autonomous-operations-agent
  template:
    metadata:
      labels:
        app: autonomous-operations-agent
    spec:
      containers:
      - name: autonomous-operations-agent
        image: ghcr.io/cmi/autonomous-operations-agent:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8002
          protocol: TCP
        - name: metrics
          containerPort: 9090
          protocol: TCP
        envFrom:
        - configMapRef:
            name: autonomous-ops-config
        resources:
          requests:
            cpu: 1000m
            memory: 1Gi
          limits:
            cpu: 4000m
            memory: 4Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2

---
apiVersion: v1
kind: Service
metadata:
  name: autonomous-operations-agent
  namespace: cmi-naap
  labels:
    app: autonomous-operations-agent
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8002
    targetPort: 8002
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: autonomous-operations-agent

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: autonomous-operations-agent-hpa
  namespace: cmi-naap
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: autonomous-operations-agent
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: autonomous-operations-agent-network-policy
  namespace: cmi-naap
spec:
  podSelector:
    matchLabels:
      app: autonomous-operations-agent
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: cmi-naap
    ports:
    - protocol: TCP
      port: 8002
    - protocol: TCP
      port: 9090
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: cmi-naap
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
